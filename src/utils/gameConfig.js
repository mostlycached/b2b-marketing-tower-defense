const gameConfig = {
  maxWaves: 10,
  startingBudget: 1000,
  pathWidth: 30,
  aiIntelligence: 0.7,   // 0-1, how smart the AI is
  aiReactionTime: 1.5,   // seconds between AI actions
  prospectStartDelay: 2000, // ms between prospect spawns
};

export default gameConfig;
